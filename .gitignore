# Workspace data directories (event logs are user data, never source)
*.db
*.db-wal
*.db-shm
churn.lock
/workspace/

# Build artifacts
/churn.exe
/dist/

# Script outputs (scripts/backup.sh, scripts/export.sh)
churn-backup-*.db
churn-export-*.jsonl

# Frontend
node_modules/

# Editor/OS noise
.DS_Store
Thumbs.db

# Claude Code: launch.json (dev server config) is shared; local settings are not
.claude/settings.local.json

# Go test binaries
*.test
*.test.exe
